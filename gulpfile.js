/**
 * Workflow
 */

// declare vars
var gulp, sass, sourcemaps, prefix, config;

// init plugins
gulp = require('gulp');
sass = require('gulp-sass');
sourcemaps = require('gulp-sourcemaps');
prefix = require('gulp-autoprefixer');